---
title: Advanced
weight: 30
description: |
  Working on the document itself: the parser, the syntax tree, tokens,
  YAMLPath queries and the JSON bridge.

  For programs that transform YAML rather than consume it.
---

The codec turns a document into Go values and forgets the document. These pages
are for the other job: reading a document's structure, its positions and its
comments, and writing a changed one back.

{{< children type="card" description="true" >}}
